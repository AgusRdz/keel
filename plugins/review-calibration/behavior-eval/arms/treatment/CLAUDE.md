# (treatment arm — intentionally no calibration block)

This file exists only so both arms have a project CLAUDE.md at the same path depth.
It deliberately contains no behavioral rules: the treatment arm relies on the model's
defaults plus the user's (un-calibrated) global CLAUDE.md.
